bin/
obj/
/packages/
riderModule.iml
/_ReSharper.Caches/

# JetBrains Rider / ReSharper
.idea/
*.DotSettings.user

# Visual Studio
.vs/
*.user
*.suo

# Misc
*.zip
*.log

# Frontend (client/) - deferred from version control for now, not part of
# this increment's commit. client/ has its own .gitignore for when it is
# committed (node_modules, .next, .env.local, etc.).
/client/

# Repomix - generated codebase dumps. repomix.config.js writes them to
# .repomix/; repomix-output.* is the tool's default when run without it.
/.repomix/
repomix-output.*
