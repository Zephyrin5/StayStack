<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
    </PropertyGroup>

    <ItemGroup>
        <!-- See docs/adr/0004 for why Contracts projects (not Bookings
             itself) are what other modules depend on, and why a domain
             exception thrown from more than one module (BookingNotPayableException)
             belongs here rather than the shared kernel. BuildingBlocks is
             only for AppException - the heavier packages it happens to
             carry for its other residents come along transitively, which
             is a packaging cost, not a boundary violation. -->
        <ProjectReference Include="..\..\..\Core\SeedWork\SeedWork.csproj"/>
        <ProjectReference Include="..\..\..\Core\BuildingBlocks\BuildingBlocks.csproj"/>
    </ItemGroup>

</Project>
